{
  "name": "pi-tsgw",
  "version": "0.1.0",
  "description": "Pi extension for the TOSSP AIH gateway: model catalog and per-model request operations for openai-completions, openai-responses, anthropic, and gemini wire protocols.",
  "type": "module",
  "keywords": [
    "pi",
    "pi-package",
    "aih",
    "gateway",
    "models"
  ],
  "license": "MIT",
  "pi": {
    "extensions": [
      "./extensions/index.ts"
    ]
  },
  "scripts": {
    "test": "tsc && node dist/extensions/index.test.js && node dist/extensions/models/operations.test.js && node dist/extensions/models/catalog.test.js && node dist/extensions/web-search/web-search.test.js"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "^0.83.0",
    "typescript": "^7.0.2"
  }
}
